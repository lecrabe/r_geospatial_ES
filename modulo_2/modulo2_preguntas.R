# Q1 -> Leer el raster disponible en una variable denominada "mi_raster"
# Q2 -> Extraer una sub-trama espacial para la zona 8.5 8.55 Este y 3.3 3.4 Norte: denominado "sub_raster"
# Q3 -> Crear una cuadrícula de 500 puntos en el clip llamado "mustreo" (Nota: Mantenga la información de coordenadas)
# Q4 -> Cambiar el nombre de la columna "muestreo" con los valores "x_coord", "y_coord" y "valor". Añadir una columna con un identificador único "id"
# Q5 -> Mostrar puntos de distribución para cada clase
# Q6 -> Extracto de "muestreo" un conjunto de 50 puntos al azar de la selva pantanosa clase. el nombre de "pts_FM"
# Q7 -> Extracto de píxeles "sub_raster" como data.frame, denominación "df_rast". Añadir una columna con un identificador único "id"
# Q8 -> Extracto de "df_rast" un conjunto de 50 puntos al azar, el nombre del "pts_rast"
# Q9 -> crear un tamaño de gráfico vacío "sub_raster" con las etiquetas "longitud" y "latitud" en ordenada
# P10 -> Show "sub_raster" en el fondo del gráfico, señala muestreo (en negro), los puntos pts_FM en verde y azules puntos pts_rast